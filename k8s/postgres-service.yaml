apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: oficina-api
  labels:
    app: oficina-api
    tier: db
    env: production
spec:
  type: ClusterIP
  selector:
    app: oficina-api
    tier: db
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
